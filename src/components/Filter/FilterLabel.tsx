import { DefaultComponentProps } from "@/shared/types";

const FilterLabel = ({ children }: DefaultComponentProps) => (
  <div className="min-w-label">{children}</div>
);

export default FilterLabel;
